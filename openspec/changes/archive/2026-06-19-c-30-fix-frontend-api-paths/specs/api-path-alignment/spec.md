## Overview

No new specifications required — this is a bugfix that aligns frontend API paths with existing backend endpoints without changing any functional requirements.

All changes are in the scope of existing specs:
- `equipos-export` — already defines `/exportar?materia_id=...&cohorte_id=...`
- `encuentros-instancias` — already defines `/instancias` path
- `liquidaciones` — backend endpoints were defined in C-18 (archived)